---
const { value } = Astro.props as { value: string };
---

<div class="story-year">{value}</div>

<style>
  .story-year {
    font-size: clamp(1.8rem, 4vw, 3rem);
    font-weight: 800;
    color: #214244;
    text-align: left;
    letter-spacing: 2px;
  }
</style>
